[pytest]
filterwarnings =
    # Ignore deprecation warnings from third-party libraries we can't fix
    ignore:Using the initialization functions in flask_caching:DeprecationWarning
    ignore:codecs.open\(\) is deprecated:DeprecationWarning
